/// <reference types="cypress" />

describe('Hooks' , () => {
    before( () => {
        // uruchamianie raz przed testami
    })

    beforeEach(()=>{
        // uruchamianie przed każdym testem
    })

    afterEach(()=>{
        // uruchamianie po każdym teście
     })

     after(()=>{
        // uruchamianie raz po testach

     })
})